import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const StoreOffer = ({navigation}) => {
  return (
    <View
      style={{
        flex: 1,
      }}>
      <Text
        style={{
          fontSize: 25,
          color: 'black',
        }}>
        StoreOffer
      </Text>
    </View>
  );
};

export default StoreOffer;

const styles = StyleSheet.create({});
