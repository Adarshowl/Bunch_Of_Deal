import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const StoreReview = ({navigation}) => {
  return (
    <View
      style={{
        flex: 1,
      }}>
      <Text
        style={{
          fontSize: 25,
          color: 'black',
        }}>
        review
      </Text>
    </View>
  );
};

export default StoreReview;

const styles = StyleSheet.create({});
